const style = (theme) => ({
  chatBox: {
    height: theme.raise.chatBoxHeight,
  },
  messages: {
    overflow: 'auto',
    flex: 'auto',
    backgroundColor: 'white',
  },
  hotkeys: {
    outline: 'none',
  }
});

export default style;
